# استقرار دوختک روی هاست اشتراکی cPanel

راهنمای گام‌به‌گام انتشار سایت روی هاست اشتراکی (PHP 8.x + MySQL + Apache).
بدون Composer/Node روی سرور. همه‌چیز PHP خام و PDO است.

---

## پیش‌نیازها
- هاست cPanel با **PHP 8.0 یا بالاتر** و افزونه‌های `pdo_mysql`، `mbstring`، `curl`، `gd` (برای تغییر اندازه تصاویر).
- یک دیتابیس MySQL/MariaDB با `utf8mb4`.
- دسترسی File Manager یا FTP، و phpMyAdmin.
- دامنه‌ی متصل‌شده (کانونیکال: `dookhtak.ir` بدون www).

## ۱) ساخت دیتابیس و کاربر
1. در cPanel → **MySQL® Databases**: یک دیتابیس بساز (مثلاً `dookhtak_site`).
2. یک کاربر با رمز قوی بساز و **ALL PRIVILEGES** روی آن دیتابیس بده.
3. نام دیتابیس/کاربر/رمز و `DB_HOST` (معمولاً `localhost`) را یادداشت کن.

## ۲) وارد کردن ساختار دیتابیس
1. phpMyAdmin → دیتابیس را انتخاب کن → تب **Import**.
2. فایل `database/schema.sql` را آپلود و اجرا کن.
   - Charset حتماً `utf8mb4` باشد. باید **۱۵ جدول** ساخته شود و داده‌های اولیه (seed) درج شود.

## ۳) آپلود فایل‌ها
1. کل پروژه را در `public_html/` (یا document root دامنه) آپلود کن.
2. مطمئن شو فایل‌های زیر آپلود شده‌اند: `.htaccess`، پوشه‌های `assets/`، `includes/`، `admin/`، `api/`، `database/`، `cache/`، `uploads/`.
3. فایل‌های محلی توسعه لازم نیست بروند: پوشه‌ی `qa/` و `.git/`.

## ۴) تنظیم config.php
1. `includes/config.example.php` را به `includes/config.php` کپی کن.
2. مقادیر را پر کن:
   - `DB_HOST`، `DB_NAME`، `DB_USER`، `DB_PASS`.
   - `BASE_URL` = `https://dookhtak.ir` (بدون اسلش انتهایی، هاست کانونیکال non-www).
   - `SESSION_NAME` را می‌توانی نگه داری.
   - `CACHE_ENABLED` = `true` (کش صفحه فعال).
   - `MOTHER_API` را فعلاً `enabled => false` بگذار تا مستندات API سامانه مادر برسد.
   - `APP_ENV` = `production` (خطاهای PHP مخفی می‌ماند).
3. `config.php` هرگز در گیت نیست و فقط روی سرور می‌ماند.

## ۵) ساخت ادمین و حذف اسکریپت
1. یک بار در مرورگر برو به `https://dookhtak.ir/database/create_admin.php` و از طریق **CLI/راهنمای داخل فایل** ادمین بساز (این اسکریپت درخواست HTTP را رد می‌کند و فقط از خط فرمان یا طبق دستور داخل فایل کار می‌کند — امن).
   - اگر SSH داری: `php database/create_admin.php` را اجرا و نام‌کاربری/رمز را وارد کن.
2. **بلافاصله بعد از ساخت ادمین، فایل `database/create_admin.php` را حذف کن.**

## ۶) دسترسی نوشتن پوشه‌ها
- `uploads/` و `uploads/images/` → قابل‌نوشتن (۷۵۵؛ اگر لازم شد ۷۷۵).
- `cache/` → قابل‌نوشتن (۷۵۵). داخلش فایل `.htaccess` با «deny all» هست؛ آن را نگه دار.
- پوشه‌های `includes/`، `database/`، `cache/` توسط `.htaccess` از دسترسی وب مسدودند.

## ۷) HTTPS
1. در cPanel یک گواهی SSL (مثلاً AutoSSL/Let’s Encrypt) نصب کن.
2. در `.htaccess` دو خط ریدایرکت HTTPS را از حالت کامنت خارج کن (توگل):
   ```apache
   RewriteCond %{HTTPS} !=on
   RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [R=301,L]
   ```

## ۸) بررسی نهایی پس از استقرار
- صفحه‌ی اصلی، `/pricing`، `/blog`، یک پست، `/contact` باز شوند (۲۰۰).
- `/sitemap.xml` و `/robots.txt` درست باشند.
- ورود به `/admin/` با ادمین ساخته‌شده.
- ارسال فرم تماس → پیام در **پیام‌های دریافتی** ادمین دیده شود.
- مسیرهای حساس مسدود: `/includes/config.php`، `/database/schema.sql`، `/cache/…` → ۴۰۳.
- `create_admin.php` حذف شده باشد (تست: باز کردن آدرسش نباید کار کند).

## ۹) سئو پس از انتشار
1. در **Google Search Console** دامنه را ثبت و تأیید کن.
   - کد تأیید را می‌توانی از پنل ادمین → **سئو → اسکریپت‌ها** داخل `<head>` بگذاری.
2. `https://dookhtak.ir/sitemap.xml` را در Search Console ثبت کن.
3. متای هر صفحه از **سئو → متای صفحات** قابل ویرایش است.

## ۱۰) کارهای اختیاری/آینده
- **WebP**: برای سرعت بیشتر می‌توان تصاویر آپلودی را به WebP هم تبدیل کرد؛ عمداً پیاده نشد تا دیزاین تأییدشده پیکسل‌به‌پیکسل حفظ شود. یک بهینه‌سازی امن پساانتشار است.
- **سامانه مادر**: با رسیدن مستندات API، بلوک `MOTHER_API` را در `config.php` پر و `enabled => true` کن. آداپتور (`includes/MotherApiClient.php`) آماده است و فرم تماس «ذخیره اول، فوروارد بعد» عمل می‌کند.

---

## نکته‌های نگه‌داری
- **کش صفحه**: برای بازدیدکننده‌های ناشناس فعال است (TTL یک ساعت) و روی **هر ذخیره‌ی ادمین** که محتوای عمومی را تغییر دهد به‌صورت خودکار پاک می‌شود. اگر خواستی دستی پاک کنی: **تنظیمات سایت → پاک‌کردن کش**.
- **بکاپ**: قبل از هر تغییر بزرگ، از دیتابیس و پوشه‌ی `uploads/` بکاپ بگیر.
- **به‌روزرسانی asset**: چون `.htaccess` کش دور و دراز روی CSS/JS می‌گذارد، هنگام تغییر یک asset نام فایل را عوض کن (یا کش مرورگر را دور بزن) تا کاربران نسخه‌ی جدید را بگیرند.
